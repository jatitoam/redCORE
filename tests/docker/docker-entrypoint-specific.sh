#!/bin/bash

export GITHUB_TOKEN="{github:token}"
export SLACK_CHANNEL="{slack:channel}"
export SLACK_TOKEN="{slack:token}"
export DOCKERTEST_PHPVERSION="{php:version}"
export DOCKERTEST_JOOMLAVERSION="{joomla:version}"
export DOCKERTEST_APPNAME="{app:name}"
